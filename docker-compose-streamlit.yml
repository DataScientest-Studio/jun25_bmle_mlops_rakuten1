services:
  streamlit:
    build:
      context: .
      dockerfile: docker/streamlit/Dockerfile
    container_name: rakuten_streamlit
    networks:
      - mlops-network
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    restart: always
